export type User = {
    role: string[],
    school_id: 1,
    name: string,
    surname: string,
    login: string,
    id: number
}

export type Journal = {
    id: number
    grade: number
    subject: string
}